---
title: "PSA Diagnostic Message Queue — Model Design Document: PSADMQ_MDD"
description: "Model Design Document for PSA Diagnostic Message Queue (summary)."
---

# PSA Diagnostic Message Queue — Model Design Document: PSADMQ_MDD

> Source: `PSADMQ/doc/PSADMQ_MDD.doc` (210,432 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`PSADMQ_MDD.doc` follows the project naming convention for a **Model Design Document** for PSA Diagnostic Message Queue (`PSADMQ`): requirements trace, signal interfaces, state/behaviour description, calibration parameters and verification references.

## How to read the original

1. Open the `.doc` file from the repository (`PSADMQ/doc/PSADMQ_MDD.doc`).
2. Check the revision table and referenced signal/configuration names against `PSADMQ/src/` and `PSADMQ/generate/`.
3. Cross-check integration steps with the sibling integration manual if present.

