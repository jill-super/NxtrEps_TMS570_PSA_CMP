---
title: "Active Pull Compensation — Integration manual: ActivePullCmp_Integration Manual"
description: "Integration manual for Active Pull Compensation (summary)."
---

# Active Pull Compensation — Integration manual: ActivePullCmp_Integration Manual

> Source: `ActivePull/doc/ActivePullCmp_Integration Manual.doc` (156,160 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`ActivePullCmp_Integration Manual.doc` follows the naming convention for an **integration manual** for Active Pull Compensation (`ActivePull`): build wiring, generation steps, configuration parameters, Runtime Environment port wiring and integration checks.

## How to read the original

1. Open the `.doc` file from the repository (`ActivePull/doc/ActivePullCmp_Integration Manual.doc`).
2. Follow the generation/integration batch files in `ActivePull/generate/` and `ActivePull/tools/`.
3. Verify port connections against the AUTOSAR description in `ActivePull/autosar/`.

