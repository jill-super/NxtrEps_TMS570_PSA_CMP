---
title: "PSA Diagnostic Service Gateway — Integration manual: PSADSG_IntegrationManual"
description: "Integration manual for PSA Diagnostic Service Gateway (summary)."
---

# PSA Diagnostic Service Gateway — Integration manual: PSADSG_IntegrationManual

> Source: `PSADSG/doc/PSADSG_IntegrationManual.doc` (131,072 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`PSADSG_IntegrationManual.doc` follows the naming convention for an **integration manual** for PSA Diagnostic Service Gateway (`PSADSG`): build wiring, generation steps, configuration parameters, Runtime Environment port wiring and integration checks.

## How to read the original

1. Open the `.doc` file from the repository (`PSADSG/doc/PSADSG_IntegrationManual.doc`).
2. Follow the generation/integration batch files in `PSADSG/generate/` and `PSADSG/tools/`.
3. Verify port connections against the AUTOSAR description in `PSADSG/autosar/`.

