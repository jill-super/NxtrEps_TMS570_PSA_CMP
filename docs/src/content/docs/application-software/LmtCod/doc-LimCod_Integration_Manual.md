---
title: "Limiter Conditioning — Integration manual: LimCod_Integration_Manual"
description: "Integration manual for Limiter Conditioning (summary)."
---

# Limiter Conditioning — Integration manual: LimCod_Integration_Manual

> Source: `LmtCod/doc/LimCod_Integration_Manual.doc` (136,704 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`LimCod_Integration_Manual.doc` follows the naming convention for an **integration manual** for Limiter Conditioning (`LmtCod`): build wiring, generation steps, configuration parameters, Runtime Environment port wiring and integration checks.

## How to read the original

1. Open the `.doc` file from the repository (`LmtCod/doc/LimCod_Integration_Manual.doc`).
2. Follow the generation/integration batch files in `LmtCod/generate/` and `LmtCod/tools/`.
3. Verify port connections against the AUTOSAR description in `LmtCod/autosar/`.

