---
title: "Motor Control (Current Mode) — Model Design Document: CurrCmd_MDD"
description: "Model Design Document for Motor Control (Current Mode) (summary)."
---

# Motor Control (Current Mode) — Model Design Document: CurrCmd_MDD

> Source: `MtrCtrl_CM/doc/CurrCmd_MDD.doc` (4,557,824 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`CurrCmd_MDD.doc` follows the project naming convention for a **Model Design Document** for Motor Control (Current Mode) (`MtrCtrl_CM`): requirements trace, signal interfaces, state/behaviour description, calibration parameters and verification references.

## How to read the original

1. Open the `.doc` file from the repository (`MtrCtrl_CM/doc/CurrCmd_MDD.doc`).
2. Check the revision table and referenced signal/configuration names against `MtrCtrl_CM/src/` and `MtrCtrl_CM/generate/`.
3. Cross-check integration steps with the sibling integration manual if present.

