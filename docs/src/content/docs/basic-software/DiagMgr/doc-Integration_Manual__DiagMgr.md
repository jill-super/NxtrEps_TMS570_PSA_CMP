---
title: "Diagnostics Manager — Integration manual: Integration Manual _DiagMgr"
description: "Integration manual for Diagnostics Manager (summary)."
---

# Diagnostics Manager — Integration manual: Integration Manual _DiagMgr

> Source: `DiagMgr/doc/Integration Manual _DiagMgr.doc` (142,336 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`Integration Manual _DiagMgr.doc` follows the naming convention for an **integration manual** for Diagnostics Manager (`DiagMgr`): build wiring, generation steps, configuration parameters, Runtime Environment port wiring and integration checks.

## How to read the original

1. Open the `.doc` file from the repository (`DiagMgr/doc/Integration Manual _DiagMgr.doc`).
2. Follow the generation/integration batch files in `DiagMgr/generate/` and `DiagMgr/tools/`.
3. Verify port connections against the AUTOSAR description in `DiagMgr/autosar/`.

