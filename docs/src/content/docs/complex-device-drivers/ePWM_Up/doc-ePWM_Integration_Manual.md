---
title: "Enhanced Pulse-Width Modulation Driver — Integration manual: ePWM_Integration_Manual"
description: "Integration manual for Enhanced Pulse-Width Modulation Driver (summary)."
---

# Enhanced Pulse-Width Modulation Driver — Integration manual: ePWM_Integration_Manual

> Source: `ePWM_Up/doc/ePWM_Integration_Manual.doc` (152,064 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`ePWM_Integration_Manual.doc` follows the naming convention for an **integration manual** for Enhanced Pulse-Width Modulation Driver (`ePWM_Up`): build wiring, generation steps, configuration parameters, Runtime Environment port wiring and integration checks.

## How to read the original

1. Open the `.doc` file from the repository (`ePWM_Up/doc/ePWM_Integration_Manual.doc`).
2. Follow the generation/integration batch files in `ePWM_Up/generate/` and `ePWM_Up/tools/`.
3. Verify port connections against the AUTOSAR description in `ePWM_Up/autosar/`.

