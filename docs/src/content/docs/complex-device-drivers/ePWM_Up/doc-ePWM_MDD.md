---
title: "Enhanced Pulse-Width Modulation Driver — Model Design Document: ePWM MDD"
description: "Model Design Document for Enhanced Pulse-Width Modulation Driver (summary)."
---

# Enhanced Pulse-Width Modulation Driver — Model Design Document: ePWM MDD

> Source: `ePWM_Up/doc/ePWM MDD.doc` (1,074,176 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`ePWM MDD.doc` follows the project naming convention for a **Model Design Document** for Enhanced Pulse-Width Modulation Driver (`ePWM_Up`): requirements trace, signal interfaces, state/behaviour description, calibration parameters and verification references.

## How to read the original

1. Open the `.doc` file from the repository (`ePWM_Up/doc/ePWM MDD.doc`).
2. Check the revision table and referenced signal/configuration names against `ePWM_Up/src/` and `ePWM_Up/generate/`.
3. Cross-check integration steps with the sibling integration manual if present.

