---
title: "Standard Software Library (Filters, Interpolation, Math) — Design document: Filter_Library_Design_Document"
description: "Design document for Standard Software Library (Filters, Interpolation, Math) (summary)."
---

# Standard Software Library (Filters, Interpolation, Math) — Design document: Filter_Library_Design_Document

> Source: `NxtrLib/doc/Filter_Library_Design_Document.doc` (370,688 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## Context

`Filter_Library_Design_Document.doc` (Filter Library Design Document) is a design artefact for Standard Software Library (Filters, Interpolation, Math) (`NxtrLib`). Open the original file in the repository for figures, tables and full text.

