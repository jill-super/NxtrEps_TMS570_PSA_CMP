---
title: "Standard Software Library (Filters, Interpolation, Math) — Model Design Document: Interpolation_Design_MDD"
description: "Model Design Document for Standard Software Library (Filters, Interpolation, Math) (summary)."
---

# Standard Software Library (Filters, Interpolation, Math) — Model Design Document: Interpolation_Design_MDD

> Source: `NxtrLib/doc/Interpolation_Design_MDD.doc` (153,600 bytes, legacy binary Word `.doc`).
>
> This format was **not converted to full text**: legacy binary Word needs a full office converter (e.g. LibreOffice), which is not available in this documentation build. What follows is a structured summary from the file name and module context — the `.doc` file in the repository remains the authoritative source.

## What this file most likely contains

`Interpolation_Design_MDD.doc` follows the project naming convention for a **Model Design Document** for Standard Software Library (Filters, Interpolation, Math) (`NxtrLib`): requirements trace, signal interfaces, state/behaviour description, calibration parameters and verification references.

## How to read the original

1. Open the `.doc` file from the repository (`NxtrLib/doc/Interpolation_Design_MDD.doc`).
2. Check the revision table and referenced signal/configuration names against `NxtrLib/src/` and `NxtrLib/generate/`.
3. Cross-check integration steps with the sibling integration manual if present.

